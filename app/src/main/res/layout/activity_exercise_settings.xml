<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:padding="24dp">

        <TextView
            android:id="@+id/txtTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Exercise Settings"
            android:fontFamily="@font/nunito_bold"
            android:textSize="30sp"
            android:textColor="@color/text_primary"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="4dp"/>

        <TextView
            android:id="@+id/txtSubtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Set difficulty level for patient"
            android:fontFamily="@font/nunito_regular"
            android:textSize="16sp"
            android:textColor="@color/text_secondary"
            android:layout_marginBottom="24dp"/>

        <!-- Current Difficulty Card -->
        <androidx.cardview.widget.CardView
            xmlns:app="http://schemas.android.com/apk/res-auto"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="28dp"
            app:cardCornerRadius="16dp"
            app:cardElevation="3dp"
            app:cardBackgroundColor="@color/surface">

            <TextView
                android:id="@+id/txtCurrentDifficulty"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Current: Medium  (5 questions)"
                android:fontFamily="@font/nunito_bold"
                android:textSize="17sp"
                android:textColor="@color/primary"
                android:gravity="center"
                android:padding="20dp"/>

        </androidx.cardview.widget.CardView>

        <!-- Easy -->
        <Button
            android:id="@+id/btnLow"
            style="@style/RoundedButton"
            android:layout_width="match_parent"
            android:layout_height="72dp"
            android:text="✅  Easy — 3 Questions"
            android:fontFamily="@font/nunito_bold"
            android:textSize="19sp"
            android:backgroundTint="#43A047"
            android:layout_marginBottom="14dp"/>

        <!-- Medium -->
        <Button
            android:id="@+id/btnMedium"
            style="@style/RoundedButton"
            android:layout_width="match_parent"
            android:layout_height="72dp"
            android:text="⚡  Medium — 5 Questions"
            android:fontFamily="@font/nunito_bold"
            android:textSize="19sp"
            android:backgroundTint="@color/warning"
            android:layout_marginBottom="14dp"/>

        <!-- Hard -->
        <Button
            android:id="@+id/btnHigh"
            style="@style/RoundedButton"
            android:layout_width="match_parent"
            android:layout_height="72dp"
            android:text="🔥  Hard — 7 Questions"
            android:fontFamily="@font/nunito_bold"
            android:textSize="19sp"
            android:backgroundTint="@color/btn_emergency"
            android:layout_marginBottom="32dp"/>

        <!-- Back -->
        <Button
            android:id="@+id/btnBack"
            style="@style/RoundedButton"
            android:layout_width="match_parent"
            android:layout_height="72dp"
            android:text="← Back"
            android:fontFamily="@font/nunito_bold"
            android:textSize="18sp"
            android:backgroundTint="@color/text_secondary"/>

    </LinearLayout>
</ScrollView>